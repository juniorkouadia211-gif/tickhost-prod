services:
  - type: web
    name: tickhost
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        value: sqlite://ticketing.db
      - key: JWT_SECRET
        generateValue: true
      - key: QR_SECRET
        generateValue: true
